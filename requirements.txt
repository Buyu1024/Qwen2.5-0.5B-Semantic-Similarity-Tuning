# === 核心依赖 ===
torch>=2.1.0
transformers>=4.43.0
datasets>=2.18.0
peft>=0.10.0
accelerate>=0.28.0

# === 数据处理 ===
pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0

# === 性能加速（可选，按需安装） ===
# Flash Attention 2 — 注意力计算 2-5x 加速（推荐 RTX 4090/5090）
# pip install flash-attn --no-build-isolation
#
# bitsandbytes — 4/8-bit 量化（显存 < 8GB 时使用）
# pip install bitsandbytes
